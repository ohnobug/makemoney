为了更贴近Vigaviga：

修改了 tabs.dart 中的第261行~275行：
return MaterialStateColor.resolveWith((Set<MaterialState> states) {
  if (states.contains(MaterialState.selected)) {
    if (animation.value >= 0.5) {
      return unselectedColor;
    } else {
      return Color.lerp(selectedColor, unselectedColor, animation.value * 0.7)!;
    }
  }

  if (animation.value >= 0.5) {
      return selectedColor;
    } else {
      return Color.lerp(unselectedColor, selectedColor, animation.value * 0.7)!;
    }
});