<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- =================================================================== -->
    <!--                            1. 核心与网络权限                         -->
    <!-- =================================================================== -->
    <!-- 访问网络 (最基础的权限) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 检查网络状态，判断是Wi-Fi还是移动数据 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许应用在后台运行时保持CPU唤醒，用于长连接等 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <!-- =================================================================== -->
    <!--                       2. 媒体、文件与存储权限                        -->
    <!-- =================================================================== -->
    <!-- 录音权限 (用于语音消息、语音/视频通话) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 修改音频设置 (例如，切换到扬声器) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 写入外部存储 (用于保存图片、文件等) -->
    <!-- 注意: 对于 Android 10 (API 29) 及以上版本，这个权限的作用已大大减弱。
         添加 maxSdkVersion="28" 是一个好习惯，表明只在旧版本上请求。 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- 读取外部存储 (用于从相册选择图片、发送文件) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Android 13 (API 33) 及以上版本，需要更精细的媒体权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />


    <!-- =================================================================== -->
    <!--                       3. 硬件访问权限 (摄像头等)                      -->
    <!-- =================================================================== -->
    <!-- 摄像头权限 (用于拍照、录像、扫一扫、视频通话) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 控制闪光灯 -->
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <!-- 震动权限 (用于新消息提醒) -->
    <uses-permission android:name="android.permission.VIBRATE" />


    <!-- =================================================================== -->
    <!--                       4. 用户隐私数据权限                          -->
    <!-- =================================================================== -->
    <!-- 精确地理位置 (用于发送位置、附近的人) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 大致地理位置 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 后台定位权限 (用于实时位置共享等功能，非常敏感) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- 读取联系人 (用于“添加手机联系人”功能) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />


    <!-- =================================================================== -->
    <!--                         5. 设备与通知权限                          -->
    <!-- =================================================================== -->
    <!-- 发送通知 (Android 13 及以上必须) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- 蓝牙权限 (Android 12 及以上需要) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- 读取手机状态 (用于在通话时中断App的语音功能) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    
    <!-- 生物识别/指纹权限 (用于支付等场景) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />


    <!-- =================================================================== -->
    <!--                         6. 特殊权限 (需要引导用户到设置页)             -->
    <!-- =================================================================== -->
    <!-- 悬浮窗权限 (用于语音/视频通话的最小化窗口) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 安装其他APK (用于应用内更新) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />


    <application
        android:label="@string/applicationLabel"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        >
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>